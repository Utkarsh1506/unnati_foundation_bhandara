import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Unnati Foundation Bhandara | Community Welfare & Development",
  description: "UNNATI BAHUUDDESHIYA KALYANKARI SAMAJIK SANSHTA BHANDARA - Empowering communities through education, women empowerment, and rural development in Maharashtra",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
